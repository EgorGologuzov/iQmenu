import { createTheme } from "@mui/material";

export const APP_THEME = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#444444',
    },
    secondary: {
      main: '#01c2cb',
    },
  },
});